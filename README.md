# Workout timer

A simple first version of a HIIT workout timer app, built with Expo.

## Start the app

```bash
npx expo start
```

Leave that terminal running. Then open the app in [Expo Go](https://expo.dev/go) on a real phone.
